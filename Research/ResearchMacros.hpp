#pragma once

#define RESULT_FILE_NAME "result.csv"

#define SIZE(w, h) {w, h}
#define BEGIN_AT(bx, by) {bx, by}
#define END_AT(ex, ey) {ex, ey}

#define REPEAT_TIMES(n) (n)
#define MAZE_FILENAME(maze) (maze)
#define GENERATE_MAZE {}

constexpr auto DRAWNING_ON{true};
constexpr auto DRAWNING_OFF{false};
